<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Detail</title>

    <style>
        body {
            margin: 50px;
            font-family: Arial, sans-serif;
        }

        .table-container {
            display: flex;
        }

        .table-header {
            width: 30%;
            margin-right: 20px;
        }

        .table-body {
            flex-grow: 1;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
        button{
            background-color: blue;
            width: 100px;
            height: 30px;
            font-size: 25px;
            float: right;
        }
        
    </style>
</head>
<body>

    <h2 style="text-align: center; font-size: 35px">User Detail</h2>

    <div class="table-container">
        <div class="table-header">
            <table>
                <thead>
                    <tr>
                        <th>ID:</th>
                    </tr>
                    <tr>
                        <th>Name:</th>
                    </tr>
                    <tr>
                    <th>Email:</th>
                    </tr>
                    <tr>
                    <th>Role:</th>
                    </tr>
                </thead>
            </table>
        </div>

        <div class="table-body">
            <table>
                <tbody>
                    <tr>
                        <td><%= @current_user.id %></td>
                    </tr>
                    <tr>
                        <td><%= @current_user.name %></td>
                    </tr>
                    <tr>
                        <td><%= @current_user.email %></td>
                    </tr>
                    <tr>
                        <td><%= @current_user.usertype %></td>
                    </tr>
                    <tr>
                    </tr>
                </tbody>
            </table>
        </div>
    </div><br>

    <%= link_to 'Users list'%>
</body>
</html>
