<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Registration Form</title>
  <style>

    h1 {
      text-align: center;
      margin: 5px 0;
      font-size: 50px;
    }

    .registration-container {
      margin: auto;
      width: 50%;
      padding: 20px;
      border-radius: 10px;
    }

    .form-label {
      font-weight: 700;
      font-size: 22px;
    }
  </style>
</head>

<body>
  <h1>Registration Form</h1>
  <div class="registration-container">
    <%= form_for @user, url: user_path(@user), method: :post, html: { class: "mb-3" } do |f| %>
      <div class="mb-3">
        <%= f.label :name, class: "form-label" %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :email, class: "form-label" %>
        <%= f.email_field :email, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :usertype, "User Type", class: "form-label" %>
        <%= f.select :usertype, [['Admin', 'admin'], ['Manager', 'manager'], ['Employee', 'employee']], { prompt: 'Select User Type' }, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :password, class: "form-label" %>
        <%= f.password_field :password, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :password_confirmation, "Confirm Password", class: "form-label" %>
        <%= f.password_field :password_confirmation, class: "form-control" %>
      </div>

      <%= f.submit "Sign Up", class: "btn btn-primary" %>
      <%= link_to "Log In", login_path, class: "btn btn-secondary ms-5" %>

    <% end %>
  </div>

</body>
</html>
